@{
    ViewBag.Title = "SignMailConfirm";
    ViewBag.UserID = BaseUtility.DataUtil.GetString(Request["userid"]);
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Login</title>
    <meta charset='UTF-8' />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="@Url.Content("~/Content/bootstrap.css")" rel="stylesheet" />
    <link href="@Url.Content("~/Content/common.css")" rel="stylesheet" />
</head>
<body>
    <div id="bd" class="page-wrapper page-wrapper-simple">
        <div id="nonav" class="page-simple">
            <div id="content">
                <div>
                    <h1 class="maintenance-title text-center">
                        @ViewBag.UserID
                    </h1>
                    <input type="button" id="btnSendMail" class="btn btn-primary" value="Send Mail" onclick="Page.FnSendMail();" />
                </div>
                
                <div>
                    <div style="text-align: right; overflow: hidden;">
                        <input type="button" id="btnConfirm" class="btn btn-primary" value="Confirm" onclick="Page.FnConfirm();" />
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
@Scripts.Render("~/bundles/jquery")
<script>
    var Page = {
        FnSendMail: function() {
            
        },
        FnConfirm: function() {
            
        }
    };

    $(function() {

    });
</script>